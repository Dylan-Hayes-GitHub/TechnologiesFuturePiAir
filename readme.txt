Install node.js from https://nodejs.org/en/download LTS version
Install angular global cli npm install -g @angular/cli
After cloning or downloading repo navigate to root directory of application through a command line
run npm i
after this is complete run ng serve, the application will build and start.
